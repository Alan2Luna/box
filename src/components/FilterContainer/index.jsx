import React from 'react';
import './FilterContainer.css';

const FilterContainer = ({ children }) => (
    <section className="filter-container">{children}</section>
)

export default FilterContainer;